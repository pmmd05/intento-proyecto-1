# Copia este archivo a .env y ajusta los valores
# Cadena de conexión para Postgres local (coincide con docker-compose.yml)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/anima_db

# JWT
JWT_SECRET=dev-secret-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
